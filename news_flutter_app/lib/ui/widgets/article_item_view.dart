import 'package:flutter/material.dart';

class ArticleItemView extends StatelessWidget {
  const ArticleItemView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
